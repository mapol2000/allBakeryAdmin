<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="sql.cmm">

	<!-- 현재 날짜 -->
	<select id="getDate" resultType="mirlab.common.vo.MirCamelVO">
		SELECT NOW() AS TO_DATE, #{loginUserId} AS USER_ID
	</select>



</mapper>